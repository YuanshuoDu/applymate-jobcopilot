export const AGENT_HARNESS_PROTOCOL_VERSION = 'agent-harness.v2'

// Keep the existing name as a compatibility alias for current protocol users.
export const schemaVersion = AGENT_HARNESS_PROTOCOL_VERSION

export const protocolRevision = 1
