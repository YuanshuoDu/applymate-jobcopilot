{
  "framework": "nextjs",
  "buildCommand": "pnpm --filter @jobcopilot/shared build && if [ \"$VERCEL_ENV\" = \"production\" ]; then pnpm --filter @jobcopilot/web exec prisma migrate deploy; fi && pnpm --filter @jobcopilot/web exec prisma generate && pnpm --filter @jobcopilot/web build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install --ignore-scripts",
  "devCommand": "pnpm --filter @jobcopilot/web dev",
  "crons": [
    {
      "path": "/api/gmail/sync/daily",
      "schedule": "15 8 * * *"
    },
    {
      "path": "/api/notifications/daily",
      "schedule": "30 8 * * *"
    },
    {
      "path": "/api/admin/audit-checkpoint",
      "schedule": "0 2 * * *"
    }
  ]
}
